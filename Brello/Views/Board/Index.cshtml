@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    
}

    <h1 class=" text-center">@ViewBag.Message</h1>
  
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
            <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
    </div>

        }
    </ul>
    </div>
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
                <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
            </div>

        }
    </ul>
</div>
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
                <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
            </div>

        }
    </ul>
</div>
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
                <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
            </div>

        }
    </ul>
</div>
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
                <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
            </div>

        }
    </ul>
</div>
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
                <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
            </div>

        }
    </ul>
</div>
<div class="row">
    <ul style="padding: 0">
        @foreach (var item in ViewBag.Things)
        {
            <div classs="col-md-4 col-md-offset-4">
                <li style="list-style:none;" class="text-center"><a href="https://google.com">foo</a></li>
            </div>

        }
    </ul>
</div>

     }

@*@using (Html.BeginForm("Create", "Board", FormMethod.Post, htmlAttributes: new { style = "display:block;", @class = "mystyle" }))
{
    @Html.AntiForgeryToken()
    @Html.TextBox("name")*@
       